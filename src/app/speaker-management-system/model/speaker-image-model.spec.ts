import { SpeakerImageModel } from './speaker-image-model';

describe('SpeakerImageModel', () => {
  it('should create an instance', () => {
    expect(new SpeakerImageModel()).toBeTruthy();
  });
});
